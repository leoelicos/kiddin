<article>
   <section>
      <img src="{{image_url}}" alt="post image">
   </section>
	<section>
		<h3>{{title}}</h3>
		<p>in {{thread.title}}</p>
	</section>
	<section>
		<p>{{date_format date}}</p>
		<p>by {{user.username}}</p>
	</section>
</article>